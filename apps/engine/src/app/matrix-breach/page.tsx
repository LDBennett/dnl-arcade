import { MatrixBreachTerminal } from "@/widgets/matrix-breach-terminal";

export const metadata = { title: "Matrix Breach — RetroArcade Engine" };

export default function MatrixBreachPage() {
  return <MatrixBreachTerminal />;
}
