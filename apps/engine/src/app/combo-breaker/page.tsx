import { ComboBreakerArena } from "@/widgets/combo-breaker-arena";

export const metadata = { title: "ComboBreaker — RetroArcade Engine" };

export default function ComboBreakerPage() {
  return <ComboBreakerArena />;
}
