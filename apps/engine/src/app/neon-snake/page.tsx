import { NeonSnakeArena } from "@/widgets/neon-snake-arena";

export const metadata = { title: "Neon Snake — RetroArcade Engine" };

export default function NeonSnakePage() {
  return <NeonSnakeArena />;
}
